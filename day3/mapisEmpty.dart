// Returns true if the Map is an empty Map
void main() {
  // when data is filled the box the output is false
  var details = {'Usrname': 'mahesh', 'Password': 'Pass@123'};
  print(details.isEmpty);

//  data is not fill so that output  is True
  var hosts = {};
  print(hosts.isEmpty);

  var admin = {'Usrname': 'mahesh', 'Password': 'Pass@123'};
  print(details.isEmpty);
}
